# 🎉 功能更新说明

**更新日期**：2026-01-13  
**版本**：v2.0 - 支持指定用户爬取

---

## ✨ 新功能：支持爬取指定用户的内容

### 核心特性

之前只能爬取**登录用户自己**的内容，现在可以：

- ✅ 爬取**自己的内容**（原有功能保留）
- ✅ 爬取**任何用户的公开内容**（新功能）
- ✅ 支持文章、回答、想法三种类型
- ✅ 保持原有的所有功能（Markdown、PDF、数学公式等）

---

## 🔧 更新内容

### 1. 新增命令行参数

**`--target_user`**

指定要爬取的用户名（知乎个人主页 URL 中的标识）

```bash
# 不指定：爬取登录用户自己的内容
python crawler.py --article --MarkDown --links_scratch

# 指定用户名：爬取该用户的公开内容
python crawler.py --article --MarkDown --links_scratch --target_user zhang-san
```

### 2. 更新启动脚本

**`run.sh`** 现在支持传递用户名参数

```bash
# 原有用法（不变）
./run.sh article        # 爬取自己的文章

# 新用法
./run.sh article zhang-san    # 爬取指定用户的文章
```

### 3. 新增文档

| 文档名称 | 说明 |
|---------|------|
| `指定用户爬取功能说明.md` | 完整功能介绍和使用指南 |
| `快速示例-指定用户爬取.md` | 实用示例和场景演示 |
| `功能更新-2026-01-13.md` | 本更新说明（当前文件）|

---

## 📖 使用方法

### 方式一：便捷脚本

```bash
# 爬取自己的内容
./run.sh article           # 自己的文章
./run.sh answer            # 自己的回答
./run.sh think             # 自己的想法
./run.sh all               # 自己的所有内容

# 爬取指定用户的内容
./run.sh article zhang-san   # 指定用户的文章
./run.sh answer li-si        # 指定用户的回答
./run.sh think wang-wu       # 指定用户的想法
./run.sh all zhao-liu        # 指定用户的所有内容
```

### 方式二：Python 命令

```bash
source venv/bin/activate

# 爬取自己的内容
python crawler.py --article --MarkDown --links_scratch

# 爬取指定用户的内容
python crawler.py --article --MarkDown --links_scratch --target_user zhang-san

# 自定义参数
python crawler.py --article --MarkDown --links_scratch \
                  --target_user li-si \
                  --sleep_time 10
```

---

## 🎯 典型场景

### 场景 1：备份自己的内容
```bash
./run.sh all
```

### 场景 2：学习知乎大 V 的文章
```bash
./run.sh article excited-vczh
```

### 场景 3：收藏多个用户的精彩回答
```bash
./run.sh answer user-a
./run.sh answer user-b
./run.sh answer user-c
```

### 场景 4：批量爬取关注的用户
```bash
for user in zhang-san li-si wang-wu; do
    ./run.sh all "$user"
    sleep 30
done
```

---

## 📝 如何获取用户名

用户名是知乎个人主页 URL 中的标识部分：

```
https://www.zhihu.com/people/zhang-san-123
                              ^^^^^^^^^^^^^ 这就是用户名
```

**步骤**：
1. 访问目标用户的知乎主页
2. 查看浏览器地址栏的 URL
3. 提取 `/people/` 后面的部分

---

## ⚠️ 重要说明

### 必须先登录

即使爬取其他用户的内容，也需要先用**自己的账号**登录：

```bash
./run.sh login
```

**原因**：
- 避免被知乎识别为未登录访问而限制
- 提高爬取成功率和稳定性
- 就像用浏览器访问别人的公开主页一样

### 只能爬取公开内容

- ✅ 可以爬取：公开的文章、回答、想法
- ❌ 不能爬取：私密内容、需要关注才能看的内容

### 遵守爬取频率

- 默认间隔：6 秒
- 建议深夜运行
- 避免给知乎服务器压力

---

## 🔍 技术实现

### 修改的文件

1. **`crawler.py`**
   - 第 1265 行：添加 `--target_user` 参数
   - 第 1191-1206 行：判断并使用指定的用户名

2. **`run.sh`**
   - 第 5-10 行：更新使用说明
   - 第 31-37 行：获取并处理用户名参数
   - 第 41-76 行：更新各个命令的逻辑

### 核心逻辑

```python
# 1. 登录并获取登录用户的用户名
driver, username = login_loadsavecookie(driver)

# 2. 判断是否指定了目标用户
if args.target_user:
    username = args.target_user  # 使用指定的用户名
    print(f"将爬取指定用户的内容: {username}")
else:
    username = username           # 使用登录用户的用户名
    print(f"将爬取登录用户自己的内容: {username}")

# 3. 构造 URL 并爬取（逻辑不变）
articles_url = f'https://www.zhihu.com/people/{username}/posts'
```

---

## 📚 相关文档

| 文档 | 用途 |
|------|------|
| `指定用户爬取功能说明.md` | 详细功能介绍、参数说明、FAQ |
| `快速示例-指定用户爬取.md` | 实用示例和场景演示 |
| `QUICKSTART.md` | 快速开始指南 |
| `README_MACOS.md` | 完整使用手册 |
| `问题已解决.md` | 故障排除指南 |

---

## 🆕 vs 🔄 功能对比

| 功能 | 之前 | 现在 |
|------|------|------|
| 爬取自己的内容 | ✅ 支持 | ✅ 支持（不变） |
| 爬取指定用户的内容 | ❌ 不支持 | ✅ **新增支持** |
| Markdown 格式 | ✅ 支持 | ✅ 支持（不变） |
| PDF 格式 | ✅ 支持 | ✅ 支持（不变） |
| 数学公式 | ✅ 支持 | ✅ 支持（不变） |
| 代码高亮 | ✅ 支持 | ✅ 支持（不变） |
| 图片保存 | ✅ 支持 | ✅ 支持（不变） |

---

## 🚀 快速开始

### 首次使用

```bash
cd /Users/zhuaoyuan/cursor-workspace/knowledge-base/scripts/zhihu_spider_selenium

# 1. 登录（必须）
./run.sh login

# 2. 开始爬取
./run.sh all                  # 爬取自己的所有内容
./run.sh all zhang-san        # 爬取指定用户的所有内容
```

### 查看帮助

```bash
# 脚本帮助
./run.sh

# Python 参数帮助
source venv/bin/activate
python crawler.py --help
```

---

## 💡 使用技巧

### 技巧 1：批量爬取多个用户

创建用户列表并批量爬取：

```bash
# 创建用户列表
cat > users.txt << EOF
zhang-san
li-si
wang-wu
EOF

# 批量爬取
while read user; do
    echo "爬取用户: $user"
    ./run.sh all "$user"
    sleep 60
done < users.txt
```

### 技巧 2：组合不同用户的内容

```bash
# 爬取用户 A 的文章
./run.sh article user-a

# 爬取用户 B 的回答
./run.sh answer user-b

# 爬取用户 C 的想法
./run.sh think user-c
```

### 技巧 3：自定义爬取参数

```bash
source venv/bin/activate

# 增加间隔时间，更加友好
python crawler.py --article --MarkDown --links_scratch \
                  --target_user zhang-san \
                  --sleep_time 10
```

---

## ❓ 常见问题

### Q: 如何找到用户名？
**A:** 访问该用户的知乎主页，查看 URL 中 `/people/` 后面的部分。

### Q: 需要对方同意吗？
**A:** 不需要。这是爬取公开内容，就像用浏览器访问一样。

### Q: 会被发现吗？
**A:** 不会。这是正常的浏览行为。

### Q: 可以爬取哪些用户？
**A:** 所有有公开主页的知乎用户，包括大 V、认证用户、普通用户。

### Q: Cookie 是谁的？
**A:** 你自己的 cookie，但爬取的是指定用户的公开内容。

---

## 📊 测试结果

已测试以下场景：

- ✅ 爬取自己的文章、回答、想法
- ✅ 爬取指定用户的文章、回答、想法
- ✅ 爬取知乎大 V 的内容
- ✅ 批量爬取多个用户
- ✅ Markdown 和 PDF 格式正常
- ✅ 数学公式、代码、图片正常保存
- ✅ 错误处理和重试机制正常

---

## 🎓 学习资源

### 了解更多

1. **功能详解** → `指定用户爬取功能说明.md`
2. **实用示例** → `快速示例-指定用户爬取.md`
3. **完整手册** → `README_MACOS.md`
4. **快速开始** → `QUICKSTART.md`

### 获取帮助

```bash
# 查看所有命令
./run.sh

# 查看所有参数
source venv/bin/activate
python crawler.py --help

# 检查环境
python test_env.py

# 检查登录
python check_login.py
```

---

## 🙏 感谢使用

这次更新增加了非常实用的功能，希望能帮助您：

- 📚 学习知乎大 V 的优质内容
- 💾 备份喜欢的用户的精彩文章
- 📖 离线阅读和学习
- 🎯 收集特定领域的知识

**开始使用：**

```bash
cd /Users/zhuaoyuan/cursor-workspace/knowledge-base/scripts/zhihu_spider_selenium
./run.sh all [用户名]
```

---

**更新完成！** 🎉

*如有问题，请查看相关文档或运行 `./run.sh` 查看帮助。*
